[phases.setup]
nixPkgs = ["nodejs_24", "pnpm", "caddy"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @todorant/web build"]

[start]
cmd = "caddy file-server --root apps/web/dist --listen :8080"
