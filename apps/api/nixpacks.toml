[phases.setup]
nixPkgs = ["nodejs_24", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @todorant/api build"]

[start]
cmd = "pnpm --filter @todorant/api db:migrate && node apps/api/dist/server.js"
